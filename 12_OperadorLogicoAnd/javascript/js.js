var idade = 18;
var temDinheiro = true;


if (idade >= 18 && temDinheiro === true) {
    console.log ("pode comprar biscoito");
} else {
    console.log ("não pode comprar biscoito");
}

/* o operador lógico && também conhecido como And , ele vai retornar true apenas se as duas expressões
retornarem true, qualquer outro resultado do operador lógico And retornará false. */